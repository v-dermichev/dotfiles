
return {
  {
    "mason-org/mason.nvim",
    opts = {},
    config = function(_, opts)
      require("mason").setup(vim.tbl_extend("force", opts, {
        registries = {
          "github:mason-org/mason-registry",   -- default registry
          "github:Crashdummyy/mason-registry", -- additional registry
        },
      }))
    end,
  },
}

